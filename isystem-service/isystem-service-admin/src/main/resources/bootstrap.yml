spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: isystem-client-eureka, isystem-service-datasource, isytem-client-redis, isystem-service-admin, isystem-common-service
      label: master
      profile: dev